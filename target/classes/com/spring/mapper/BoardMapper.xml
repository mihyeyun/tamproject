<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.BoardMapper">
	
	<select id="getList" resultType="com.spring.domain.BoardVO">
		select * from tam_board order by bno desc
	</select>
	
	<insert id="insert">
		insert into tam_board (bno, title, content, writer) values (seq_board.nextval, #{title}, #{content}, #{writer})
	</insert>
	
	<select id="read" resultType="com.spring.domain.BoardVO">
		select * from tam_board where bno = #{bno}
	</select>
	
	<delete id="delete">
		delete from tam_board where bno = #{bno}
	</delete>
	
	<update id="update">
		update tam_board set title = #{title}, content = #{content}, writer = #{writer}, updatedate = sysdate where bno = #{bno}
	</update>

</mapper>